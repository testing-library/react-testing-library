---
kind: pipeline
type: docker
name: test

concurrency:
  limit: 1

steps:
  - name: test
    image: node:lts-slim
    commands:
      - npm run setup
      - npm run test
trigger:
  event:
    - pull_request
    - rollback
